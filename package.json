{
  "name": "kugou",
  "version": "1.0.0",
  "description": "kugou",
  "main": ".prettierrc.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky",
    "precommit:angular": "cd demo/angular && npm run precommit",
    "precommit": "npm-run-all precommit:*"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "husky": "^9.1.7",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.6.2"
  }
}
