{
  "name": "e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test:prepare": "npm i && npx playwright install --with-deps",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:angular": "playwright test --project=angular",
    "test:react-vite": "playwright test --project=react-vite",
    "test:vue3": "playwright test --project=vue3",
    "test:all": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^20.0.0",
    "dotenv": "^16.4.0",
    "typescript": "^5.0.0"
  }
}

